defaults:
  - default
  - override dataset: polar_lows
  - override model: segformer # smp
  - override lr_scheduler: redplat
  - override loss: dice_ce_mse

logger:
  use_env_token: True
  project: andreaf/polarlows

batch_size: 16

checkpoints: True

run_tags: ['polar-lows-seg']