defaults:
  - default
  - override dataset: cancer
  - override model: segformer
  - override lr_scheduler: redplat
  - override loss: dice_ce #

logger:
  use_env_token: True
  project: andreaf/polarlows

checkpoints: True

predict:
  stage: test
  test_image_dir: data/bus/test_images
  gt_folder: data/bus/gt_masks

run_tags: ['cancer']
